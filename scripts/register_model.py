#!/usr/bin/env python3
"""Register a trained nnU-Net model folder (equivalent to `microct-register-model`)."""
from microct_lab.cli import register_model_cli

if __name__ == "__main__":
    register_model_cli()
